use serde::{Deserialize, Serialize};
use crate::utils::{AuthorConfig, Config, GitHubConfig, ModelConfig};
use std::path::Path;


pub fn watch_model(no_log: bool) {

    let config_path = Path::new(".modm/config.json");

}